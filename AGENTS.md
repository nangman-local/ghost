# GHOST

딴짓하고 있는 순간, 먼저 찾아와 흐름을 끊어주는 집중 동반자.

이 파일은 이 저장소에서 작업하는 모든 AI 코딩 도구(Claude, Codex 등)가 공통으로 참고하는 컨텍스트입니다. 팀원 전원이 같은 아키텍처·상태 정의·규칙을 공유해야 각자의 AI가 만든 코드가 서로 충돌하지 않습니다.

## 핵심 컨셉

사용자가 집중 앱을 켜는 것이 아니라, **집중이 무너지는 순간 서비스가 사용자를 찾아간다.**

```
할 일 인식 → 현재 사용 행동 감지 → 딴짓 여부 판단 → 딴짓 지속 시간 확인
→ 플로팅 캐릭터 개입 → 현재 할 일로 복귀 유도
```

핵심 가설: 딴짓 중인 순간 캐릭터가 먼저 개입하면, 사용자가 원래 할 일로 더 잘 복귀한다.

## 저장소 구조

```
ghost/
├── AGENTS.md         이 파일
├── android/           Android 앱 (Kotlin + Jetpack Compose)
├── desktop/            Windows PC 앱 (Electron)
├── server/             백엔드 (Spring Boot + PostgreSQL/Supabase)
└── shared/
    ├── rules-dictionary.json   딴짓/집중/모호 앱 규칙 사전
    └── api-schema/             세션/할 일/이벤트/judge API 스키마
```

각 폴더의 세부 담당과 진행 상태는 폴더별 `README.md`를 참고합니다.

## 기술 스택

| 영역 | 스택 | 비고 |
| --- | --- | --- |
| Android | Kotlin + Jetpack Compose | 포그라운드 서비스, `UsageStatsManager`, `AccessibilityService`, 오버레이(`WindowManager`) |
| PC | Electron (Windows) | `get-windows`로 활성 창 감지, 투명·항상 위·클릭 통과 창으로 캐릭터 표시 |
| 캐릭터 | Rive | `.riv` 파일 1개를 Android와 PC(웹 런타임)가 공용으로 사용 |
| 서버 | Spring Boot + PostgreSQL (또는 Supabase) | 인증, 세션 동기화, `/judge`, 이벤트 로그, 지표 |
| 실시간 통신 | PC는 WebSocket, 폰은 필요할 때 조회 | |
| AI | 경량 LLM API (Gemini Flash-Lite / GPT mini / Claude Haiku급) | 반드시 서버 경유. 클라이언트 코드에 API 키 포함 금지 |

## 시스템 구조

```
[Android 앱]                          [Windows PC 앱 (Electron)]
 - 현재 앱 / URL / 제목 감지            - 활성 창 앱명 / 창 제목 감지
 - 플로팅 캐릭터 (Rive)                 - 투명 오버레이 캐릭터 (Rive)
 - 개입 상태머신                        - 개입 상태머신
 - 캘린더 읽기
        │ REST (하트비트·이벤트)              │ WebSocket
        └──────────────┬──────────────────┘
                       ▼
                 [서버]
   - 인증 / 할 일 / 세션 (진실의 원천)
   - /judge : 규칙 사전 + LLM 판단 + 캐시
   - Notion 연동, 이벤트 로그, 지표 집계
                       │
                       ▼
                   [LLM API]
```

핵심 원칙:

1. **서버의 세션이 진실의 원천이다.** 현재 할 일, 상태(집중/딴짓/대기), 활성 기기, 마지막 하트비트를 서버가 관리한다.
2. **각 기기는 30초마다 하트비트를 보낸다.** 끊기면 해당 기기는 비활성으로 처리한다.
3. **딴짓 판단 로직은 서버 `/judge` 하나로 통일한다.** Android와 PC가 같은 판단을 공유하고, 기기에는 명확한 규칙 캐시만 둔다.
4. **기기 간 개입은 단순하게 구현한다.** 폰이 딴짓 앱을 감지하면 서버에 "PC에서 집중 세션 중인가?"를 조회하고, 그렇다면 즉시 개입한다. 동시에 PC는 WebSocket으로 이벤트를 받아 캐릭터가 반응한다.
5. **데이터 모델은 처음부터 동기화 전제로 설계한다.** 로컬 저장 위주로 만들면 나중에 구조를 뜯어고쳐야 한다.

## 상태 정의

세션 상태(`state`): `WAITING`(할 일 미시작/대기) · `FOCUS`(할 일 관련 행동 중) · `DISTRACT`(딴짓 중)

개입 단계(`interventionLevel`): `0`(없음) · `1`(환기) · `2`(복귀 제안) · `3`(화면 개입) — 4단계(강제 잠금)는 MVP 범위에서 제외

활성 기기(`activeDevice`): `PC` · `ANDROID`

세션 데이터 예시:

```json
{
  "sessionId": "s_123",
  "userId": "u_1",
  "task": { "id": "t_9", "title": "자료구조 과제", "source": "CALENDAR" },
  "state": "FOCUS",
  "activeDevice": "PC",
  "devices": [
    { "type": "PC", "lastHeartbeat": "2026-10-01T20:15:30+09:00" },
    { "type": "ANDROID", "lastHeartbeat": "2026-10-01T20:15:12+09:00" }
  ],
  "interventionLevel": 0,
  "distractSeconds": 0
}
```

## 딴짓 판단 (`/judge`)

1. 할 일 등록 시 LLM을 1회 호출해 "할 일 프로필"을 생성한다 (실시간이 아니라 등록 시점에 AI 비용을 쓴다).
2. 기기 로컬에서 규칙 판단(앱 사전 + 앱 카테고리 + 할 일 프로필)으로 집중/딴짓/모호 중 하나로 1차 분류한다. 인스타 릴스, 틱톡, 게임 등 명확한 케이스는 여기서 끝난다.
3. 모호한 경우(브라우저, YouTube, 커뮤니티 앱 등)만 서버 `/judge`를 호출한다. 입력은 할 일/앱 이름/도메인/제목, 출력은 `{ label, confidence }`이며 결과는 캐시한다.
4. 판정이 한 번으로 끝나지 않고 일정 시간 누적될 때만 개입한다. 사용자가 "이거 할 일 관련이야"를 교정하면 즉시 허용 목록에 반영한다.

개발 순서 원칙: MVP는 2번(규칙 판단)만으로 먼저 동작하게 만들고, AI(1·3번)는 이후에 얹는다. AI가 늦어져도 제품은 동작해야 한다.

판단 원칙:
- 오탐이 미탐보다 훨씬 치명적이다. 3단계(화면 개입)는 규칙으로 확정됐거나 AI confidence가 높을 때만 허용한다.
- 모호하면 판단하지 말고 캐릭터가 사용자에게 직접 물어본다.
- AI 호출은 최소화한다 (앱/제목이 15~30초 이상 유지될 때만 호출).
- AI가 실패해도 동작해야 한다 (오류·오프라인 시 규칙만으로 판단, 모호하면 개입하지 않음).
- 짧은 행동(예: 카카오톡 확인)은 딴짓 시간에 누적하지 않는다.
- 서버로는 도메인과 제목만 보내고, URL 쿼리스트링과 화면 텍스트는 보내지 않는다.

## 코딩·협업 규칙

- **API 키는 서버에만 둔다.** `android/`, `desktop/` 코드에 LLM/외부 API 키를 절대 넣지 않는다.
- **크리티컬 패스(감지·오버레이·상태머신) 변경은 Android 코어 오너가 확인 후 머지한다.**
- **`shared/`의 공통 정의(상태 정의, API 스키마, 규칙 사전)를 바꿀 때는 팀 공유 채널에 공지한다.**
- **실기기에서 확인한 것만 완료로 인정한다.** 에뮬레이터 동작은 완료가 아니며, 삼성 기기 최소 1대에서 확인이 필요하다.
- Android 12 이상은 불투명 오버레이 아래로 터치가 전달되지 않는다. "콘텐츠 일부 가리기"는 가린 영역만 터치를 막고 나머지 화면은 정상 동작하도록 구현한다("클릭을 완전히 막지 않는다" 원칙).

## 역할 분담

| 역할 | 담당 포지션 | 담당자 | 주요 업무 |
| --- | --- | --- | --- |
| Android 코어 오너 | 풀스택 | 정수 | 감지 서비스, 오버레이, 개입 상태머신 (크리티컬 패스) |
| Android 서브 | 백엔드 A | 현기 | 캐릭터 애니메이션 연동, 권한·배터리 대응, 캘린더 연동, 실기기 테스트 |
| AI 판단 | 백엔드 B | 진형 | 서버 `/judge`(규칙 사전 + LLM), 할 일 프로필 생성, Task Breakdown, 일정→할 일 변환, 평가셋 |
| 서버·동기화 | 백엔드 C | 경민 | 인증, 세션 동기화 API, 하트비트, WebSocket, Notion OAuth, 이벤트 로그·지표 |
| 프론트 | 프론트 | 정호 | W1~2 Android 앱 화면(Compose) / W3~ Electron PC 클라이언트 |
| UX/UI | 웹디자이너 | 호연 | 앱 UI, PC 설정 창, 권한 온보딩 플로우, 데모 영상, 발표자료 |
| 캐릭터 | 캐릭터 디자이너 | 보민 | Rive 캐릭터 1종 + 상태별 모션, 모바일·PC 공용 |

## MVP 범위 (제외된 것)

앱 강제 잠금, 내기·보상 시스템은 이번 프로젝트에서 구현하지 않는다(발표에서 "향후 확장"으로만 소개). Notion 연동과 Body Doubling은 10/4 Go/No-Go 판단에 따라 조건부로 진행한다.
