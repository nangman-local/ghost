# GHOST

딴짓하고 있는 순간, 먼저 찾아와 흐름을 끊어주는 집중 동반자.

이 파일은 팀 공통 헌법이다. 모든 AI 코딩 도구(Claude, Codex 등)가 공통으로 참고한다.
**세부 규칙은 각 폴더의 `AGENTS.md`에, 공통 정의는 `shared/`에 있다. 중복해서 쓰지 않는다.**

## 핵심 컨셉

사용자가 집중 앱을 켜는 것이 아니라, **집중이 무너지는 순간 서비스가 사용자를 찾아간다.**

```
할 일 인식 → 사용 행동 감지 → 딴짓 판단 → 지속 시간 확인 → 캐릭터 개입 → 복귀 유도
```

핵심 가설: 딴짓 중인 순간 캐릭터가 먼저 개입하면, 사용자가 원래 할 일로 더 잘 복귀한다.

## 저장소 구조

```
ghost/
├── AGENTS.md      이 파일 (팀 공통 헌법)
├── shared/        단일 진실의 원천 — 바꾸면 팀 공유 채널에 공지
│   ├── api-schema.md        세션/할일/judge API 입출력
│   ├── states.md            세션 상태, 개입 단계, 캐릭터 상태 이름
│   ├── distract-rules.json  딴짓 앱 규칙 사전
│   └── events.md            지표 계산용 이벤트 정의
├── android/       Kotlin + Jetpack Compose
├── desktop/       Electron (Windows)
├── server/        Spring Boot + PostgreSQL/Supabase
└── web/           랜딩·대시보드 (Vercel Root Directory 지정 대상)
```

## 아키텍처 원칙

1. **서버의 세션이 진실의 원천이다.** 현재 할 일, 상태, 활성 기기, 마지막 하트비트를 서버가 관리한다. 클라이언트의 로컬 상태가 서버 세션을 대체하지 않는다.
2. **각 기기는 30초마다 하트비트를 보낸다.** 끊기면 해당 기기는 비활성으로 처리한다.
3. **딴짓 판단 로직은 서버 `/judge` 하나로 통일한다.** 기기에는 규칙 캐시만 둔다.
4. **데이터 모델은 처음부터 동기화 전제로 설계한다.** 로컬 저장 위주로 만들면 나중에 뜯어고쳐야 한다.
5. **AI가 실패해도 제품은 동작해야 한다.** MVP는 규칙 판단만으로 먼저 완성하고 AI를 나중에 얹는다.

기술 스택: Rive 캐릭터 `.riv` 1개를 Android·PC 공용. PC는 WebSocket, 폰은 필요할 때 조회. AI는 경량 LLM API(Gemini Flash-Lite / GPT mini / Claude Haiku급)를 **반드시 서버 경유**로 호출한다.

## 딴짓 판단 흐름

1. 할 일 등록 시 LLM 1회 호출로 "할 일 프로필" 생성 (실시간 아님 — 등록 시점에 비용을 쓴다)
2. 기기 로컬에서 `shared/distract-rules.json`으로 1차 분류. 인스타·틱톡·게임 등 명확한 케이스는 여기서 끝
3. 모호한 경우만 서버 `/judge` 호출. 결과는 캐시한다
4. 판정이 일정 시간 누적될 때만 개입한다

**판단 원칙**

- **오탐이 미탐보다 훨씬 치명적이다.** 3단계(화면 개입)는 규칙으로 확정됐거나 AI confidence가 높을 때만.
- 모호하면 판단하지 말고 캐릭터가 사용자에게 직접 물어본다.
- AI 호출은 최소화한다 (앱/제목이 15~30초 이상 유지될 때만).
- 짧은 행동(카카오톡 확인 등)은 딴짓 시간에 누적하지 않는다.
- 사용자가 "이거 할 일 관련이야"를 교정하면 즉시 허용 목록에 반영한다.

## 작업 흐름

**시작 전:** 이 파일 + 작업할 폴더의 `AGENTS.md` 를 읽는다. 상태 이름·API를 쓴다면 `shared/` 도 확인한다.

**작업:** 이슈 먼저 만들고 `develop` 에서 `타입/이슈번호-설명` 브랜치를 판다. 커밋은 `타입: 설명 (#이슈번호)`.

**작업 후:** 코드를 바꿨으면 **해당 폴더의 `AGENTS.md` 를 갱신한다.** 새로 알게 된 제약·결정사항을 적는다. CI가 검사하며 갱신이 없으면 PR이 실패한다.

브랜치 전략·커밋 컨벤션·PR 규칙 상세는 [`CONTRIBUTING.md`](./CONTRIBUTING.md).

## 절대 규칙

- **API 키는 서버에만 둔다.** `android/`, `desktop/`, `web/`에 LLM/외부 API 키를 절대 넣지 않는다.
- **서버로는 도메인과 제목만 보낸다.** URL 쿼리스트링과 화면 텍스트는 보내지 않는다.
- **크리티컬 패스(감지·오버레이·상태머신) 변경은 Android 코어 오너 확인 후 머지한다.**
- **`shared/` 변경은 팀 공유 채널에 공지한다.** 세 클라이언트를 동시에 갱신해야 한다.
- **실기기에서 확인한 것만 완료로 인정한다.** 에뮬레이터는 완료가 아니며 삼성 기기 최소 1대 확인이 필요하다.

## 역할 분담

| 역할 | 담당자 | 주요 업무 |
| --- | --- | --- |
| Android 코어 오너 | 정수 | 감지 서비스, 오버레이, 개입 상태머신 (크리티컬 패스) |
| Android 서브 | 현기 | 캐릭터 연동, 권한·배터리 대응, 캘린더, 실기기 테스트 |
| AI 판단 | 진형 | `/judge`(규칙+LLM), 할 일 프로필, Task Breakdown, 평가셋 |
| 서버·동기화 | 경민 | 인증, 세션 API, 하트비트, WebSocket, Notion OAuth, 지표 |
| 프론트 | 정호 | W1~2 Android Compose 화면 / W3~ Electron PC 클라이언트 |
| UX/UI | 호연 | 앱 UI, PC 설정 창, 권한 온보딩, 데모 영상, 발표자료 |
| 캐릭터 | 보민 | Rive 캐릭터 1종 + 상태별 모션, 모바일·PC 공용 |

## MVP 범위

앱 강제 잠금(개입 4단계), 내기·보상 시스템은 구현하지 않는다 (발표에서 "향후 확장"으로만 소개).
Notion 연동과 Body Doubling은 10/4 Go/No-Go 판단에 따라 조건부로 진행한다.
