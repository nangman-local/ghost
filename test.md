# GHOST 테스트 가이드

> **기준일 2026-09-23** · 대상 빌드: 개입 상태머신 구현(#61) + 세션 단일화(#52)
> 결과는 **GitHub 이슈에 기록**합니다. 이슈 번호는 각 항목에 적어 뒀습니다.

**완료 기준: 실기기에서 확인한 것만 완료입니다.** 에뮬레이터는 완료가 아니고, 삼성 기기 최소 1대 확인이 필요합니다(루트 `AGENTS.md`).

---

## 0. 먼저 읽을 것 — 지금 어디까지 됐나

오해를 막기 위해 먼저 적습니다. **이 세 가지는 버그가 아니라 아직 안 만든 것입니다.**

| 현상 | 이유 |
| --- | --- |
| **개입이 일어나도 화면에 아무 변화가 없다** | 캐릭터 모션(#16)과 말풍선(#63)이 아직 없습니다. 값은 **더보기 → 개발자 도구**에서 읽습니다 |
| **크롬·유튜브·카톡에서는 개입하지 않는다** | `AMBIGUOUS`로 분류돼 **판단하지 않습니다.** 의도된 동작입니다 (오탐 > 미탐) |
| **앱을 껐다 켜면 할 일이 사라진다** | 로컬 영속화 미구현(#53). 세션은 프로세스 메모리에만 있습니다 |

서버 연동도 아직 없습니다(#14). 데이터는 기기 밖으로 나가지 않습니다.

---

## 1. 설치 (전원 공통)

APK 두 종류가 있습니다.

| 파일 | 임계값 | 용도 |
| --- | --- | --- |
| `ghost-demo.apk` | 30초 / 60초 | **로직 확인용.** 5분이면 체크리스트 한 바퀴 |
| `ghost-v1.0.0-rc.apk` | 10분 / 15분 | **실사용 조건 검증용.** 기본값 |

**처음에는 데모 APK로 로직을 보고, 그다음 기본값 APK로 실제 체감을 봅니다.**

### 설치 절차 — 두 단계 모두 해야 합니다

sideload 설치라 Android가 두 번 막습니다. 둘 다 정상이며, 악성코드 때문이 아닙니다(점검 결과는 아래 "왜 경고가 뜨나" 참고).

**① 설치 전 — Play 프로텍트 끄기** (#73)

```
설정 → 보안 및 개인정보 보호 → 추가 보안 설정
→ Google Play 프로텍트 → 우측 상단 ⚙️ → "앱 검사" 끄기
→ APK 설치 → 설치 후 다시 켜기
```

안 하면 **"기기 보호를 위해 앱 차단됨"** 이 뜨고 설치가 안 됩니다.

**② 설치 후 — 제한된 설정 허용** (#74)

```
설정 → 애플리케이션 → Ghost → 우측 상단 ⋮ → "제한된 설정 허용"
```

안 하면 접근성 설정에서 **"GHOST Chrome 감지 테스트"가 회색으로 비활성**이라 켤 수 없습니다. Android 13+ 정책입니다.
→ 삼성 Tab S6 Lite / One UI에서 이 경로로 해제되는 것을 확인했습니다.

**③ 온보딩에서 권한 4개 허용**

다른 앱 위에 표시 → Chrome 접근성 → 사용 정보 접근 → 알림(Android 13+)

> 접근성을 건너뛰어도 앱은 동작하지만 **크롬 감지가 빠집니다.** 사용 정보 접근을 건너뛰면 **앱 감지 자체가 안 됩니다.**

### 왜 경고가 뜨나 (테스터에게 설명할 때 쓰세요)

디버그 키 서명 + 스토어 밖 설치 + 접근성 권한, 이 셋이 겹쳐서 Android가 기계적으로 막는 것입니다. 앱 내용과 무관합니다. 점검 결과:

- 권한 **5개뿐**이고 **`INTERNET` 권한이 없습니다** — 데이터를 밖으로 보내는 것이 원천적으로 불가능
- 서드파티 라이브러리 **0개** (AndroidX·JUnit 등 공식만), 광고·분석 SDK 없음
- 위험 API(`Runtime.exec`·`reflect`·`SmsManager`·`ContactsContract` 등) **0건**
- 접근성 서비스는 `packageNames="com.android.chrome"` — **크롬에만** 붙습니다

---

## 2. 사용자로서 체크할 것 (전원 공통)

> **역할과 무관하게 전원이 합니다.** 도그푸딩이 목적이라 "되는지"보다 **"쓸 만한지"**를 봅니다.
> 기능 체크는 3장의 담당별 항목에서 합니다.

### 2-1. 첫인상 — 온보딩

- [ ] 권한을 4개나 요구하는데, **왜 필요한지 납득이 되는가**
- [ ] 중간에 그만두고 싶어지는 지점이 있는가 (몇 번째 단계인지 적어 주세요)
- [ ] "나중에"를 눌렀을 때 앱이 어떻게 되는지 예상이 되는가
- [ ] 캐릭터가 **감시자처럼 느껴지는가, 동반자처럼 느껴지는가**

> ⚠️ **알려진 문제(#74):** 접근성 단계 문구가 "지금 어떤 앱을 사용하고 있는지…"인데, 이건 사용 정보 접근이 하는 일입니다. 접근성은 크롬 도메인·제목을 읽습니다. 문구 수정 예정이니 이 부분은 빼고 봐 주세요.

### 2-2. 실제로 써 보기 — 최소 2시간

**기본값 APK로** 평소처럼 쓰면서 봅니다.

- [ ] 실제 할 일을 하나 넣고 시작해 본다
- [ ] 평소처럼 딴짓을 한다 (일부러 참지 마세요)
- [ ] **개입이 언제 왔는지, 그때 기분이 어땠는지** 적는다

기록할 것:

| 항목 | 적을 내용 |
| --- | --- |
| 개입 시점 | 너무 빨랐나 / 늦었나 / 적당했나 |
| 오탐 여부 | **집중하고 있는데 딴짓으로 판정한 적이 있나** ← 가장 중요 |
| 미탐 여부 | 딴짓인데 아무 반응 없던 앱이 있나 (앱 이름 적어 주세요) |
| 짜증 | 앱을 지우고 싶어진 순간이 있었나 |

> **오탐이 미탐보다 훨씬 치명적입니다.** 집중 중에 방해받으면 사용자는 앱을 지웁니다.
> 오탐을 발견하면 **어떤 앱에서, 무슨 할 일이었는지** 꼭 적어 주세요. `shared/distract-rules.json` 수정 근거가 됩니다(#13).

### 2-3. 임계값 체감 (#10) — 내일부터 다 같이

**현재 값은 전부 임시값입니다.** 여러분이 써 보고 정합니다.

| 항목 | 현재 임시값 | 어떤가요? |
| --- | --- | --- |
| 1단계 환기 | 딴짓 10분 | 너무 늦다 / 적당 / 너무 빠르다 |
| 2단계 복귀 제안 | 딴짓 15분 | |
| 딴짓으로 인정 | 60초 | 카톡 확인 같은 짧은 이탈이 걸리나요? |
| 복귀 인정 | 집중 30초 | 돌아왔는데 개입이 안 풀린 적 있나요? |

- [ ] 위 표를 채워서 **#10에 코멘트**로 남긴다

### 2-4. 배터리·성능

- [ ] 하루 쓰고 나서 배터리 소모가 체감되는가
- [ ] 기기가 느려지거나 발열이 있는가
- [ ] **앱이 혼자 죽어 있던 적이 있는가** (있다면 몇 시간 뒤였는지)

---

## 3. 담당별 체크리스트

### 3-1. Android 코어 오너 — 개입 상태머신 (#69)

**가장 중요한 검증입니다.** 핵심 가설이 실제로 도는지 보는 항목입니다.

**데모 APK**로 합니다. 값은 **더보기 → 개발자 도구**의 세션 패널에서 읽습니다.

```
세션 (개입 상태머신)
상태: DISTRACT · 개입 1단계
할 일: diagnostic
```

준비: 딴짓 앱 1개 + 집중 앱 1개가 기기에 있어야 합니다.

- **딴짓**: 인스타그램 / 틱톡 / X / 페이스북 / 넷플릭스 / 네이버웹툰 / 카카오페이지 / 쿠팡 / 당근마켓 / 클래시오브클랜 / 와일드리프트
- **집중**: Google Docs / 구글 캘린더 / Keep / 클래스룸 / Notion / MS Word / Adobe Reader

| # | 할 일 | 기대 결과 |
| --- | --- | --- |
| 1 | 홈에서 할 일 추가 → 시작하기 → 딴짓 앱 **30초** | 개입 **1단계** |
| 2 | 그대로 30초 더 | 개입 **2단계** |
| 3 | 5분 넘게 계속 딴짓 | **2단계 유지** (3단계로 안 감 — MVP는 `maxLevel=2`) |
| 4 | 집중 앱으로 가서 **5초 이상** | 레벨 **0**, 상태 `FOCUS` |
| 5 | 딴짓 → 집중 3초만 → 다시 딴짓 | 누적 **이어짐** (리셋 안 됨) |
| 6 | 크롬 켜고 2분 | **개입 없음** (`AMBIGUOUS`) |
| 7 | 그만하기 | 상태 `WAITING`, 레벨 0 |
| 8 | 개발자 도구에서 시작 → 홈 확인 | 홈도 **진행 중** (#52) |
| 9 | 홈에서 시작 → 개발자 도구 확인 | 종료 버튼 활성 (#52) |

**기본값 APK로 한 번 더** (시간만 다르고 나머지는 같습니다):
- [ ] 10분 → 1단계 / 15분 → 2단계

### 3-2. Android 서브 — 권한·배터리·생존 (#69, #70)

**폰에서 해 주세요.** 태블릿은 삼성 백그라운드 제약이 약해서 아래 항목의 의미가 달라집니다.

**서비스 생존** — 10/4 Go/No-Go의 판정 기준입니다.

- [ ] 감지 서비스가 **1시간 이상** 죽지 않는가
- [ ] 배터리 최적화 예외를 **안 걸어도** 버티는가 (걸어야 한다면 온보딩에 단계 추가 필요)
- [ ] 최근 앱에서 밀어 지운 뒤 동작 (자동 재시작은 **없는 것이 정상**)
- [ ] 잠금/해제 반복 후에도 감지가 이어지는가

**화면 꺼짐 (#70) — 정책이 아직 없습니다**

- [ ] 딴짓 앱을 켠 채로 화면을 끄고 **30분 뒤** 켠다
- [ ] 누적 딴짓 시간이 **얼마로 찍히는가** ← 이 값을 그대로 #70에 적어 주세요
- [ ] 자고 일어났을 때 누적이 비정상적으로 크지 않은가

> 상태머신이 벽시계 시간으로 누적하기 때문에, 화면이 꺼진 동안 어떻게 되는지가 **정의되어 있지 않습니다.** 관측 결과로 정책을 정합니다.

**권한 예외 경로**

- [ ] 온보딩에서 권한을 **전부 건너뛰고** 앱을 써 본다 → 어떤 기능이 빠지는지 확인
- [ ] 사용 중에 설정에서 권한을 **철회**한다 → 서비스가 정리되고 오류가 표시되는가 (자동 재시도는 없는 것이 정상)
- [ ] 알림 권한 거부 시 포그라운드 서비스 알림이 안 보이는가

### 3-3. 프론트 (PC / Electron) — 상태머신 값 합의 (#10, #5, #6)

PC도 **같은 전이 규칙**을 써야 합니다. Android만 구현된 상태라 값을 맞춰야 합니다.

- [ ] `shared/states.md`의 "개입 전이 규칙" 표를 읽는다
- [ ] 이 값들을 PC에서 그대로 쓸 수 있는지 판단한다 (PC는 창 제목만 얻을 수 있어 조건이 다릅니다)
- [ ] 다르게 가야 한다면 **#10에 근거와 함께** 코멘트

PC 자체 검증:

- [ ] `npm install && npm start` — Windows에서 활성 창 감지가 되는가
- [ ] 투명 오버레이 캐릭터가 뜨고 **캐릭터 밖 클릭이 통과**되는가
- [ ] VS Code 전환이 감지되는가 (#8)

### 3-4. 서버·동기화 — 아직 검증할 것 없음 (#11, #14)

서버는 **스캐폴드 상태**입니다(환경 구축 + `/health`만). 세션 API·하트비트·WebSocket 미구현이라 앱과 연동되지 않습니다.

지금 할 것:

- [ ] Android 세션이 **기기 안에서 하나로 합쳐진 것**을 확인한다 (#52) — 하트비트를 붙일 대상이 정해졌습니다
- [ ] `shared/api-schema.md`의 `devices[].state`와 `FocusSnapshot`이 맞는지 본다
- [ ] `FocusSnapshot`에 `distractSeconds`를 추가하는 건(#71) contract 변경이라 **같이 논의** 필요

### 3-5. AI 판단 — 규칙 사전 정확도 (#13, #12)

**지금이 규칙 사전을 고칠 최적기입니다.** 도그푸딩에서 오탐·미탐이 나옵니다.

- [ ] 2장의 도그푸딩을 하면서 **오탐·미탐 앱 목록**을 모은다
- [ ] `shared/distract-rules.json`에 없어서 `AMBIGUOUS`로 빠진 앱을 기록한다
- [ ] 특히 **삼성 기본 앱·한국 앱** 커버리지를 본다 (현재 목록은 50개 내외)
- [ ] 결과를 #13에 코멘트

> 서버 `/judge`(#12)가 없는 동안은 **규칙 사전만으로** 판단합니다. 사전에 없으면 전부 `AMBIGUOUS`가 되어 개입하지 않습니다.

### 3-6. UX/UI — 화면·문구 (#54, #56, #23, #74)

- [ ] 온보딩 5화면(시작 + 권한 4개)이 실기기에서 **레이아웃이 깨지지 않는가** (태블릿·폴드 포함)
- [ ] 홈 3상태 전환이 자연스러운가 (할 일 없음 → 오늘의 할 일 → 진행 중)
- [ ] '나의 기록'의 **딴짓 시간이 계속 0으로 보입니다** — 누적값이 아직 contract에 없어서입니다(#71). 값이 붙었을 때의 표시 방식을 정해 주세요
- [ ] 다크 테마에서 읽기 어려운 곳이 있는가
- [ ] 앱 시작 시 흰 화면이 번쩍이지 않는가

**문구 (#23, #74)**

- [ ] 접근성 단계 문구가 실제 역할(크롬 도메인·제목)과 안 맞습니다 → 대안 문구 제안
- [ ] 사용 정보 접근 단계와 설명이 겹칩니다 → 두 권한의 차이가 드러나게
- [ ] Play 정책상 **요청 전에** 무엇을 읽고 무엇을 안 읽는지 고지해야 합니다

**미정 항목 (#54)**

- [ ] 홈의 진행률·체크포인트(10/30/50%)가 **자리표시 값**입니다. 시간 기준인지 작업 단계 기준인지 정해 주세요
- [ ] 온보딩 2번 그림에 제조사 배경화면이 들어 있습니다 (#56) — 교체 필요

### 3-7. 캐릭터 — 상태 이름 확인 (#16)

`.riv` 파일이 아직 없어 PNG로 대체 중입니다. **최장 리드타임 항목입니다.**

- [ ] `shared/states.md`의 캐릭터 상태 4개를 확인한다: `IDLE` / `ALERT` / `TALK` / `BLOCK`
- [ ] Rive 파일의 상태머신 입력값 이름을 **이것과 똑같이** 만든다
- [ ] 작은 크기(플로팅)에서도 상태 구분이 되는지 본다
- [ ] 개입 1·2단계에서 각각 어떤 모션일지 정한다

> 상태머신이 완성돼도 **캐릭터가 없으면 데모가 안 됩니다.** 개입 단계는 전부 캐릭터 모션으로 표현됩니다.

---

## 4. 결과 기록

### 어디에 적나

| 내용 | 위치 |
| --- | --- |
| 개입 상태머신 체크리스트 (3-1) | #69 |
| 화면 꺼짐 누적 관측값 (3-2) | #70 |
| 임계값 체감 (2-3) | #10 |
| 오탐·미탐 앱 목록 (3-5) | #13 |
| 새 버그 | **새 이슈** (`[Bug]` 템플릿) |

### 버그를 적을 때

```
기기: 삼성 Galaxy S__ / Android __ / APK: demo 또는 rc
증상:
재현 방법: 1. 2. 3.
기대 동작:
스크린샷:
```

**기기 모델과 OS 버전을 꼭 적어 주세요.** 삼성 백그라운드 제약은 기기·OS 버전마다 다릅니다.

---

## 5. 자주 나오는 질문

**Q. 개입이 안 떠요**
A. 순서대로 확인하세요. ① 딴짓 앱이 위 목록에 있나 (없으면 `AMBIGUOUS`라 개입 안 함) ② 홈의 "시작하기"로 세션을 시작했나 ③ 개발자 도구 세션 패널에 `DISTRACT`로 찍히나 ④ 사용 정보 접근 권한을 허용했나

**Q. 개입 단계가 올라갔는데 화면에 아무것도 안 떠요**
A. 정상입니다. 캐릭터 모션(#16)·말풍선(#63)이 아직 없습니다.

**Q. 크롬에서 유튜브를 봐도 개입을 안 해요**
A. 정상입니다. 크롬은 `AMBIGUOUS`라 판단하지 않습니다. 서버 `/judge`(#12)가 붙어야 맥락 판단이 가능합니다.

**Q. 앱을 껐다 켜니 할 일이 사라졌어요**
A. 로컬 영속화 미구현입니다(#53).

**Q. 빌드가 `* What went wrong: 25`로 실패해요**
A. JDK 25를 쓰고 있습니다. 17이나 21로 바꾸세요. `export JAVA_HOME=$(/usr/libexec/java_home -v 17)`

**Q. 10분/15분 기다리기 힘들어요**
A. 데모 APK를 쓰세요 (30초/60초).
